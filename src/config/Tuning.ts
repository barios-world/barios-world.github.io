/** Every game-feel number lives here. The debug panel edits these live. */
export const T = {
  GRAVITY: 1800,
  FALL_MULT: 1.6,
  APEX_MULT: 0.55,
  APEX_WINDOW: 60,
  JUMP_V: 620,
  JUMP_CUT: 0.4,
  COYOTE_MS: 100,
  BUFFER_MS: 120,
  WALK_MAX: 130,
  RUN_MAX: 240,
  GROUND_ACC: 1600,
  GROUND_FRIC: 1800,
  AIR_ACC: 900,
  AIR_DRAG: 400,
  TURN_BOOST: 1.6,
  TERMINAL_V: 900,
  RUN_THRESHOLD: 0.6,
  CAM_LERP: 0.12,
  CAM_LOOKAHEAD: 36,
  CAM_DEADZONE_W: 60,
  CAM_DEADZONE_H: 40,
  HITSTOP_MS: 60,
  STICK_RADIUS: 46,
  STICK_DEADZONE: 0.12,
  // combat
  KNOCKBACK_X: 240,
  KNOCKBACK_Y: 300,
  INVULN_MS: 1500,
  STOMP_BOUNCE: 0.62,
  COMBO_WINDOW: 1500,
  CUP_SPEED: 400,
  CUP_GRAVITY: 380,
  CUP_COOLDOWN: 320,
  // meistersager
  MOB_WALK: 55,
  MOB_RUN: 150,
  MOB_NOTICE: 180,
  MOB_ATTACK_RANGE: 80,
  MOB_TELEGRAPH: 500,
  MOB_LOSE: 360,
  MOB_RESPAWN_MS: 20000,
  WAVE_SPEED: 260,
  WAVE_LIFE: 560,
  // forms
  PUNCH_RANGE: 40,
  PUNCH_COOLDOWN: 260,
  COMBO_WINDOW_PUNCH: 700,
  FLING_VX: 380,
  FLING_VY: 220,
  SPRAY_RANGE: 74,
  CONFUSE_MS: 3000,
  DJ_PUSH: 260,
  DJ_BEAT_WINDOW: 0.08,
  CHORD_RADIUS: 110,
  CHORD_COOLDOWN: 2500,
  KHUSRA_CARD: 6,
  KHUSRA_KILL: 22,
  SPECIAL_KAFFEE_MS: 10000,
  SPECIAL_BUECHER_MS: 6000,
  SPECIAL_CAMBIO_MS: 8000,
  // hazards & variants
  VENT_LIFT: 760,
  BALL_EVERY: 3200,
  DRUM_BUFF: 1.5,
  FAHNE_BUFF: 1.4,
  FAHNE_RADIUS: 170,
};

export type TuningKey = keyof typeof T;
export const DEFAULTS: Record<TuningKey, number> = { ...T };

/** key -> [min, max, step] for the debug sliders */
export const TUNING_META: Partial<Record<TuningKey, [number, number, number]>> = {
  GRAVITY: [600, 3000, 50], FALL_MULT: [1, 2.5, 0.05], APEX_MULT: [0.2, 1, 0.05], APEX_WINDOW: [0, 200, 10],
  JUMP_V: [300, 900, 10], JUMP_CUT: [0.1, 1, 0.05], COYOTE_MS: [0, 250, 10], BUFFER_MS: [0, 250, 10],
  WALK_MAX: [60, 240, 10], RUN_MAX: [120, 400, 10], GROUND_ACC: [400, 4000, 100], GROUND_FRIC: [400, 4000, 100],
  AIR_ACC: [200, 2000, 50], AIR_DRAG: [0, 1500, 50], TURN_BOOST: [1, 3, 0.1],
  CAM_LERP: [0.02, 0.3, 0.01], CAM_LOOKAHEAD: [0, 100, 4], CAM_DEADZONE_W: [0, 200, 10], CAM_DEADZONE_H: [0, 200, 10],
  STICK_RADIUS: [24, 90, 2], STICK_DEADZONE: [0, 0.4, 0.02], RUN_THRESHOLD: [0.3, 0.95, 0.05],
  KNOCKBACK_X: [0, 500, 20], KNOCKBACK_Y: [0, 500, 20], INVULN_MS: [300, 3000, 100], STOMP_BOUNCE: [0.2, 1, 0.02],
  CUP_SPEED: [150, 700, 10], CUP_GRAVITY: [0, 1200, 20], MOB_WALK: [20, 150, 5], MOB_RUN: [60, 300, 10],
  MOB_NOTICE: [60, 400, 10], MOB_ATTACK_RANGE: [30, 200, 5], MOB_TELEGRAPH: [100, 1200, 50], WAVE_SPEED: [100, 500, 10],
};

export const GAME_H = 360;
export const TILE = 32;
